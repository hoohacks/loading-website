/*
 * The two channels every glitch surface splits into, and the violet the
 * drifting points are drawn in. Bare `r, g, b` triples because they are
 * interpolated into rgba() strings on a canvas and into CSS filter lists.
 */
export const FUCHSIA = '232, 121, 249';
export const INDIGO = '79, 70, 229';
export const VIOLET = '139, 92, 246';
